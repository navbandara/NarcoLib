# Labels Directory

This directory is designated for storing text files (`.txt`) containing labels/classes corresponding to the outputs of the TensorFlow Lite models.

## Format
- One label per line, corresponding to the class index of the model's output tensor.
