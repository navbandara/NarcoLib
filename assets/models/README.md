# Models Directory

This directory is designated for storing TensorFlow Lite (TFLite) models (`.tflite` format) used for machine learning inference in the NarcoLib app.

## File Naming Conventions
- Model files should have clear, versioned names (e.g., `mobilenet_v2_1.0_224_quant.tflite`).
